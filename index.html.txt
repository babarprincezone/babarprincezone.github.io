<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Your Name — Personal Website</title>
  <meta name="description" content="Personal website of Your Name" />
  <style>
    :root{
      --bg: #0b1020;
      --card: rgba(255,255,255,0.08);
      --card2: rgba(255,255,255,0.12);
      --text: rgba(255,255,255,0.92);
      --muted: rgba(255,255,255,0.70);
      --line: rgba(255,255,255,0.14);
      --accent: #7c5cff;
      --accent2:#18e0d0;
      --shadow: 0 20px 60px rgba(0,0,0,0.45);
      --radius: 18px;
      --max: 1080px;
      --font: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Apple Color Emoji","Segoe UI Emoji";
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family:var(--font);
      color:var(--text);
      background:
        radial-gradient(1000px 600px at 10% 10%, rgba(124,92,255,0.35), transparent 60%),
        radial-gradient(900px 600px at 90% 0%, rgba(24,224,208,0.25), transparent 55%),
        radial-gradient(1000px 700px at 80% 90%, rgba(124,92,255,0.18), transparent 60%),
        var(--bg);
      overflow-x:hidden;
    }
    a{color:inherit; text-decoration:none}
    .wrap{max-width:var(--max); margin:0 auto; padding:26px 18px 80px;}
    .nav{
      position:sticky; top:0; z-index:50;
      backdrop-filter: blur(12px);
      background: linear-gradient(to bottom, rgba(11,16,32,0.85), rgba(11,16,32,0.25));
      border-bottom:1px solid var(--line);
    }
    .nav-inner{
      max-width:var(--max);
      margin:0 auto;
      padding:14px 18px;
      display:flex; align-items:center; justify-content:space-between;
      gap:14px;
    }
    .brand{
      display:flex; align-items:center; gap:10px; font-weight:700;
      letter-spacing:0.2px;
    }
    .dot{
      width:12px; height:12px; border-radius:999px;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      box-shadow: 0 0 0 4px rgba(124,92,255,0.18);
    }
    .links{
      display:flex; gap:14px; align-items:center; flex-wrap:wrap;
      font-size:14px;
    }
    .pill{
      padding:8px 12px;
      border:1px solid var(--line);
      border-radius:999px;
      background: rgba(255,255,255,0.06);
      transition: transform .15s ease, background .15s ease, border-color .15s ease;
    }
    .pill:hover{transform: translateY(-1px); background: rgba(255,255,255,0.10); border-color: rgba(255,255,255,0.22);}
    .btn{
      padding:10px 14px;
      border-radius:999px;
      border:0;
      cursor:pointer;
      color: #07121a;
      font-weight:700;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      box-shadow: 0 12px 30px rgba(124,92,255,0.25);
      transition: transform .15s ease;
    }
    .btn:hover{transform: translateY(-1px)}
    header{
      margin-top:34px;
      display:grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap:20px;
      align-items:stretch;
    }
    @media (max-width: 900px){
      header{grid-template-columns:1fr}
    }
    .hero{
      border:1px solid var(--line);
      border-radius:var(--radius);
      background: linear-gradient(180deg, rgba(255,255,255,0.10), rgba(255,255,255,0.05));
      box-shadow: var(--shadow);
      padding:26px;
      position:relative;
      overflow:hidden;
    }
    .hero::before{
      content:"";
      position:absolute; inset:-120px -60px auto auto;
      width:320px; height:320px;
      background: radial-gradient(circle at 30% 30%, rgba(24,224,208,0.35), transparent 60%);
      filter: blur(10px);
      transform: rotate(25deg);
    }
    .kicker{color:var(--muted); font-size:14px; letter-spacing:0.25px}
    h1{margin:10px 0 10px; font-size:42px; line-height:1.1}
    @media (max-width: 520px){ h1{font-size:34px;} }
    .subtitle{color:var(--muted); font-size:16px; line-height:1.6; max-width:60ch}
    .hero-actions{display:flex; gap:12px; flex-wrap:wrap; margin-top:16px}
    .ghost{
      padding:10px 14px;
      border-radius:999px;
      border:1px solid var(--line);
      background: rgba(255,255,255,0.05);
      transition: background .15s ease, transform .15s ease, border-color .15s ease;
      font-weight:600;
    }
    .ghost:hover{background: rgba(255,255,255,0.10); transform: translateY(-1px); border-color: rgba(255,255,255,0.22);}
    .side{
      border:1px solid var(--line);
      border-radius:var(--radius);
      background: rgba(255,255,255,0.06);
      box-shadow: var(--shadow);
      padding:22px;
      display:flex;
      flex-direction:column;
      justify-content:space-between;
      gap:18px;
    }
    .card-title{font-weight:800; margin:0 0 8px}
    .small{color:var(--muted); font-size:14px; line-height:1.6; margin:0}
    .stack{display:flex; flex-wrap:wrap; gap:10px; margin-top:12px}
    .chip{
      border:1px solid var(--line);
      background: rgba(255,255,255,0.05);
      padding:8px 10px;
      border-radius:999px;
      font-size:13px;
      color: rgba(255,255,255,0.82);
    }
    section{margin-top:24px}
    .grid{
      display:grid;
      grid-template-columns: repeat(3, 1fr);
      gap:16px;
    }
    @media (max-width: 900px){ .grid{grid-template-columns:1fr} }
    .box{
      border:1px solid var(--line);
      border-radius:var(--radius);
      background: rgba(255,255,255,0.06);
      box-shadow: var(--shadow);
      padding:18px;
      transition: transform .15s ease, background .15s ease, border-color .15s ease;
    }
    .box:hover{transform: translateY(-2px); background: rgba(255,255,255,0.10); border-color: rgba(255,255,255,0.22);}
    .box h3{margin:0 0 8px; font-size:18px}
    .box p{margin:0; color:var(--muted); line-height:1.6; font-size:14px}
    .row{
      display:flex; justify-content:space-between; align-items:center; gap:10px;
      margin-top:14px; flex-wrap:wrap;
      border-top:1px solid var(--line);
      padding-top:14px;
    }
    .footer{
      margin-top:34px;
      color:var(--muted);
      font-size:13px;
      display:flex; justify-content:space-between; gap:12px; flex-wrap:wrap;
      border-top:1px solid var(--line);
      padding-top:18px;
    }
    input, textarea{
      width:100%;
      background: rgba(255,255,255,0.05);
      border:1px solid var(--line);
      color: var(--text);
      border-radius:14px;
      padding:12px 12px;
      font-family:inherit;
      outline:none;
    }
    textarea{min-height:110px; resize:vertical}
    label{font-size:13px; color: var(--muted)}
    .form{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap:12px;
    }
    @media (max-width: 700px){ .form{grid-template-columns:1fr} }
    .full{grid-column: 1 / -1;}
    .notice{font-size:13px; color:var(--muted); margin-top:10px}
  </style>
</head>
<body>

  <div class="nav">
    <div class="nav-inner">
      <div class="brand">
        <div class="dot"></div>
        <div>Your Name</div>
      </div>
      <div class="links">
        <a class="pill" href="#about">About</a>
        <a class="pill" href="#projects">Projects</a>
        <a class="pill" href="#contact">Contact</a>
        <button class="btn" id="copyEmailBtn" type="button">Copy Email</button>
      </div>
    </div>
  </div>

  <div class="wrap">
    <header>
      <div class="hero">
        <div class="kicker">Personal website</div>
        <h1>Hi, I’m <span style="background: linear-gradient(135deg, var(--accent), var(--accent2)); -webkit-background-clip:text; background-clip:text; color:transparent;">Your Name</span>.</h1>
        <p class="subtitle">
          Write 1–2 lines about yourself here. Example: “I’m learning web design and building simple websites.
          I like technology, business, and creative work.”
        </p>

        <div class="hero-actions">
          <a class="btn" href="#projects">See My Work</a>
          <a class="ghost" href="#contact">Contact Me</a>
          <a class="ghost" href="https://www.linkedin.com/" target="_blank" rel="noreferrer">LinkedIn</a>
        </div>

        <div class="row">
          <div class="small">📍 Location: Your City</div>
          <div class="small">💼 Role: Your Role</div>
          <div class="small">✨ Goal: Your Goal</div>
        </div>
      </div>

      <div class="side" id="about">
        <div>
          <h2 class="card-title">About me</h2>
          <p class="small">
            This is your “About” section. You can write a short story:
            who you are, what you do, and what you want to learn or achieve.
          </p>

          <div class="stack">
            <span class="chip">Skill 1</span>
            <span class="chip">Skill 2</span>
            <span class="chip">Skill 3</span>
            <span class="chip">Skill 4</span>
          </div>
        </div>

        <div>
          <h3 class="card-title" style="margin-bottom:8px;">Quick links</h3>
          <p class="small" style="margin-bottom:10px;">Add your links here:</p>
          <div class="stack">
            <a class="pill" href="https://github.com/" target="_blank" rel="noreferrer">GitHub</a>
            <a class="pill" href="https://www.instagram.com/" target="_blank" rel="noreferrer">Instagram</a>
            <a class="pill" href="https://x.com/" target="_blank" rel="noreferrer">X (Twitter)</a>
          </div>
        </div>
      </div>
    </header>

    <section id="projects">
      <h2 style="margin:0 0 12px;">Projects</h2>
      <div class="grid">
        <div class="box">
          <h3>Project One</h3>
          <p>A short description of your project. What did you make? Why is it useful?</p>
        </div>
        <div class="box">
          <h3>Project Two</h3>
          <p>Add another project. It can be anything: business, design, study, work, etc.</p>
        </div>
        <div class="box">
          <h3>Project Three</h3>
          <p>Put your best thing here. You can also add screenshots later.</p>
        </div>
      </div>
    </section>

    <section id="contact">
      <h2 style="margin:0 0 12px;">Contact</h2>
      <div class="box">
        <p class="small" style="margin-bottom:12px;">
          This form doesn’t send messages automatically (that needs a backend).
          But it helps you collect the message and open your email app.
        </p>

        <form id="contactForm">
          <div class="form">
            <div>
              <label>Your name</label>
              <input id="name" placeholder="Type your name" required />
            </div>
            <div>
              <label>Your email</label>
              <input id="email" type="email" placeholder="you@email.com" required />
            </div>
            <div class="full">
              <label>Message</label>
              <textarea id="message" placeholder="Write your message..." required></textarea>
            </div>
          </div>

          <div class="row">
            <button class="btn" type="submit">Open Email App</button>
            <button class="ghost" type="button" id="clearBtn">Clear</button>
            <div class="small">Email: <span id="myEmailText">you@example.com</span></div>
          </div>
          <div class="notice">Tip: change <b>you@example.com</b> to your real email inside the code.</div>
        </form>
      </div>
    </section>

    <div class="footer">
      <div>© <span id="year"></span> Your Name</div>
      <div>Made with simple HTML + CSS</div>
    </div>
  </div>

  <script>
    // ====== CHANGE THIS TO YOUR EMAIL ======
    const MY_EMAIL = "you@example.com";

    document.getElementById("myEmailText").textContent = MY_EMAIL;

    document.getElementById("year").textContent = new Date().getFullYear();

    // Copy email button
    document.getElementById("copyEmailBtn").addEventListener("click", async () => {
      try {
        await navigator.clipboard.writeText(MY_EMAIL);
        alert("Email copied: " + MY_EMAIL);
      } catch (e) {
        alert("Could not copy. Email is: " + MY_EMAIL);
      }
    });

    // Contact form -> opens default email app
    document.getElementById("contactForm").addEventListener("submit", (e) => {
      e.preventDefault();
      const name = document.getElementById("name").value.trim();
      const email = document.getElementById("email").value.trim();
      const message = document.getElementById("message").value.trim();

      const subject = encodeURIComponent("Website message from " + name);
      const body = encodeURIComponent(
        "Name: " + name + "\n" +
        "Email: " + email + "\n\n" +
        message
      );

      window.location.href = `mailto:${MY_EMAIL}?subject=${subject}&body=${body}`;
    });

    // Clear button
    document.getElementById("clearBtn").addEventListener("click", () => {
      document.getElementById("PRINCE").value = "";
      document.getElementById("BABARPRINCEOFFICIAL#GMAIL.COM").value = "";
      document.getElementById("Hello World").value = "";
    });
  </script>
</body>
</html>
